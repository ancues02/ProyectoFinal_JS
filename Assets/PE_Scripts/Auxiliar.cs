using System.Collections;
using System.Collections.Generic;
using uAdventure.Core;
using uAdventure.Runner;
using UnityEngine;
using UnityEngine.UI;

public class Auxiliar : MonoBehaviour
{
    //No Singleton
   /* BetManager betManager;
    
    private void Start()
    {

        betManager = BetManager.instance;
    }

    public void setQuinielaActual(FilaQuiniela fq)
    {
        betManager.setQuinielaActual(fq);
    }

    public void Apuesta(float apuesta)
    {
        betManager.Apuesta(apuesta);
        betManager.getQuinielaActual().ConfirmaApuesta();
    }

    public void ModifyBetMenu(bool mod)
    {
        betManager.betPannel.SetActive(mod);
    }

    public void changeScene(string name)
    {
        betManager.changeScene(name);
    }*/

}
