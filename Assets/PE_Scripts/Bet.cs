using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;


public class Bet : MonoBehaviour
{
    /*public float value;
    public Text apuestaText;

    private void Start()
    {
        value = BetManager.instance.apuestaMinima;
        apuestaText.text = value.ToString();
    }

    public void ModifyValue(float mod)
    {
        value = Mathf.Clamp(value + mod, BetManager.instance.apuestaMinima, BetManager.instance.dineroActual);
        Debug.Log(value);
        apuestaText.text = value.ToString();
    }

    public void ConfirmaApuesta()
    {
        BetManager.instance.Apuesta(value);
        value = BetManager.instance.apuestaMinima;
        apuestaText.text = value.ToString();
    }*/
}
